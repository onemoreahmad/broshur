<div class="min-h-screen bg-{{ options.primaryColor ?? 'primary' }}-900 relative overflow-hidden" x-data="{ menuOpen: false }" x-effect="document.body.style.overflow = menuOpen ? 'hidden' : 'auto'">
    <!-- Blurred Background -->
    <div class="absolutex fixed inset-0 z-0 bg-[conic-gradient(at_top,_var(--tw-gradient-stops))] from-{{ options.primaryColor ?? 'primary' }}-800 via-{{ options.primaryColor ?? 'primary' }}-700/80 to-{{ options.primaryColor ?? 'primary' }}-500">
       
        <svg class="absolute opacity-80 inset-0 -z-10 size-full mask-[radial-gradient(100%_100%_at_top_right,white,transparent)] stroke-white/5" aria-hidden="true">
            <defs>
            <pattern id="983e3e4c-de6d-4c3f-8d64-b9761d1534cc" width="200" height="200" x="50%" y="-1" patternUnits="userSpaceOnUse">
                <path d="M.5 200V.5H200" fill="none"></path>
            </pattern>
            </defs> 
            <svg x="50%" y="-1" class="overflow-visible fill-white/5">
            <path d="M-200 0h201v201h-201Z M600 0h201v201h-201Z M-400 600h201v201h-201Z M200 800h201v201h-201Z" stroke-width="0"></path>
            </svg>
            <rect width="100%" height="100%" stroke-width="0" fill="url(#983e3e4c-de6d-4c3f-8d64-b9761d1534cc)"></rect>
        </svg>
        
        {# <img src="https://images.unsplash.com/photo-1555215695-3004980ad54e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80" 
             alt="BMW Cars" 
             class="w-full h-screen fixed object-cover blur-sm opacity-20"> #}
    </div>

       

    <!-- Contact Card -->
    <div class="relative md:my-4 z-10 flex flex-col items-center md:max-w-2xl md:rounded-2xl w-full md:border-4 border-black/20 mx-auto">
        
        
        <!-- Header Image -->
        <div class="w-full min-h-[8rem] object-cover md:rounded-t-xl bg-[radial-gradient(ellipse_at_bottom_right,_var(--tw-gradient-stops))] from-[#001f3f] via-[#083358] to-[#0da574] relative">
          
            {% if header.cover %}
            <img src="{{ storage(header.cover) }}" 
                alt="BMW Cars Header" 
                class="w-full h-48 object-cover md:rounded-t-xl shadow-lg">
            {% endif %}
        </div>
         
        <div class="md:max-w-2xl w-full Xborder-b-4 border-white/90">
  
            <!-- Contact Card -->
            <div class="bg-white">

                <!-- Contact Information -->
                <div class="p-4 ">
                    <!-- Name, Title and Logo Row -->
                    <div class="flex items-center gap-x-3 mb-5">
   
                        <!-- Logo -->
                        <img src="{{ tenant('logo') }}" alt="{{ tenant('name') }}" class="size-14 rounded-xl flex items-center justify-center bg-white shadow-sm object-cover">
                          
                        <!-- Name and Title -->
                        <div class="flex-1">
                            <h1 class="text-xl font-bold text-gray-900 mb-1" >{{ tenant('name') }}
                                <small class="text-gray-500 text-lg ms-3 font-normal">
                                    {# المدينة المنورة  #}
                                🇸🇦</small>

                            </h1>
                            <p class="text-gray-600 text-sm mb-1">{{ tenant('meta.slogan.'~locale()) }}</p>
                        </div>
                   
                    </div>
 
                    
                    {# {% include 'theme::blocks.cta' %} #}
                    {{renderBlock('cta')}}

 
                </div>
            </div>
        </div>
        
        <!-- Contact Card -->
        <div class="bg-white md:rounded-b-2xl border-t-4 min-h-[30vh] pb-12 border-black/5 md:max-w-2xl w-full overflow-hidden  ">
            {{renderBlock('hero')}}
            {# {{renderBlock('about')}} #}
            {{renderBlock('features')}}
            {{renderBlock('faq')}}
        </div>

    </div>

    <footer class="pb-6 pt-2 px-2 flex items-center  justify-between mx-auto max-w-2xl w-full gap-x-4 relative z-50">
        <div class="text-xs font-normal text-center text-white/60 items-center  gap-x-4 hidden md:flex">
              الحقوق محفوظة © {{dt('Y')}} -  {{ tenant('name') }}
        </div>
     
        {# <div>
            {% if site('socialLinks') | length %}
            <div class="flex items-center gap-x-4">
                {% for link in site('socialLinks') %}
                <a href="{{link.url}}" target="_blank"
                class="flex items-center  text-black/30  p-1.5  rounded-full bg-black/5 hover:text-black/40">
                    <span class="h-5 w-5">{% include 'assets/icons/social/brand-'~link.network~'.svg' ignore missing %}</span>
                </a>
                {% endfor %}
            </div>
            {% endif %}
        </div> #}
    
        <div class="flex items-center gap-x-2 text-xs text-white/60">
            باستخدام
            <a href="https://broshur.com" target="_blank" class="cursor-pointer items-center w-full inline-flex gap-x-2 font-semibold text-[#219EBC] hover:text-[#3daac5]">
                <img class=" h-5" src="{{asset('assets/images/logo.png')}}" alt="{{site('name')}}" /> بروشور
            </a>
        </div>
    </footer>

    
</div>

 